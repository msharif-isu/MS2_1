<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/rounded_corners">


    <Button
        android:id="@+id/updateProfilePicture"
        android:layout_width="187.5dp"
        android:layout_height="60dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="30px"
        app:cornerRadius="5dp"
        android:text="Change profile picture"
        android:textColor="@color/mint_cream"
        android:backgroundTint="@color/Seagreen" />

    <Button
        android:id="@+id/deleteProfilePicture"
        android:layout_width="187.5dp"
        android:layout_height="60dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/updateProfilePicture"
        android:layout_marginBottom="10px"
        app:cornerRadius="5dp"
        android:text="Delete current picture"
        android:textColor="@color/mint_cream"
        android:backgroundTint="@color/Seagreen" />

    <ImageButton
        android:id="@+id/backButton"
        android:src="?attr/homeAsUpIndicator"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="@+id/deleteProfilePicture"
        android:background="?attr/selectableItemBackgroundBorderless"/>


</androidx.constraintlayout.widget.ConstraintLayout>